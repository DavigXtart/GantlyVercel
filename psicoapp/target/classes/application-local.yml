spring:
  datasource:
    url: jdbc:mysql://localhost:3306/gantly?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
    username: root
    password: 1234
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false
    properties:
      hibernate:
        format_sql: true
  mail:
    host: smtp.gmail.com
    port: 587
    username: userapprovalgantly@gmail.com
    password: xznhetmckdadxeib # Contraseña de aplicación de Gmail
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
          connectiontimeout: 5000
          timeout: 5000
          writetimeout: 5000

springdoc:
  api-docs:
    enabled: true
    path: /v3/api-docs
  swagger-ui:
    enabled: true
    path: /swagger-ui.html
    groups-order: DESC
  group-configs:
    - group: psicoapp
      paths-to-match: /**

logging:
  level:
    org.hibernate.SQL: debug
    org.hibernate.orm.jdbc.bind: trace

# Stripe (opcional - configurar más tarde cuando lo necesites)
# Descomenta y configura estas propiedades cuando vayas a usar Stripe:
# stripe:
#   secret:
#     key: sk_test_... # Tu clave secreta de Stripe
#   public:
#     key: pk_test_... # Tu clave pública de Stripe
#   webhook:
#     secret: whsec_... # Secreto del webhook (opcional)

app:
  base:
    url: http://localhost:5173
